[package]
name = "burrete-compute-core"
version = "0.1.0"
edition = "2021"

[dependencies]
burrete-compute-protocol = { path = "../burrete-compute-protocol" }

[dev-dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
